<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.jee.modules.quartz.dao.QuartzJobLogDao">
    
	<sql id="quartzJobLogColumns">
		a.id AS "id",
		a.name AS "name",
		a.type AS "type",
		a.start_date AS "startDate",
		a.end_date AS "endDate",
		a.status AS "status",
		a.create_date AS "createDate",
		a.create_by AS "createBy.id",
		a.update_date AS "updateDate",
		a.update_by AS "updateBy.id",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="quartzJobLogJoins">
	</sql>
    
	<select id="get" resultType="QuartzJobLog">
		SELECT 
			<include refid="quartzJobLogColumns"/>
		FROM quartz_job_log a
		<include refid="quartzJobLogJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="QuartzJobLog">
		SELECT 
			<include refid="quartzJobLogColumns"/>
		FROM quartz_job_log a
		<include refid="quartzJobLogJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="type != null and type != ''">
				AND a.type = #{type}
			</if>
			<if test="beginStartDate != null and endStartDate != null and beginStartDate != '' and endStartDate != ''">
				AND a.start_date BETWEEN #{beginStartDate} AND #{endStartDate}
			</if>
			<if test="beginEndDate != null and endEndDate != null and beginEndDate != '' and endEndDate != ''">
				AND a.end_date BETWEEN #{beginEndDate} AND #{endEndDate}
			</if>
			<if test="status != null and status != ''">
				AND a.status = #{status}
			</if>
			<if test="beginUpdateDate != null and endUpdateDate != null and beginUpdateDate != '' and endUpdateDate != ''">
				AND a.update_date BETWEEN #{beginUpdateDate} AND #{endUpdateDate}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="QuartzJobLog">
		SELECT 
			<include refid="quartzJobLogColumns"/>
		FROM quartz_job_log a
		<include refid="quartzJobLogJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO quartz_job_log(
			id,
			name,
			type,
			start_date,
			end_date,
			status,
			create_date,
			create_by,
			update_date,
			update_by,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{name},
			#{type},
			#{startDate},
			#{endDate},
			#{status},
			#{createDate},
			#{createBy.id},
			#{updateDate},
			#{updateBy.id},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE quartz_job_log SET 	
			name = #{name},
			type = #{type},
			start_date = #{startDate},
			end_date = #{endDate},
			status = #{status},
			update_date = #{updateDate},
			update_by = #{updateBy.id},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE quartz_job_log SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>